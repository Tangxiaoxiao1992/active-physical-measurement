本轮被执行工具故障阻塞：启动时报 `os error 2`（找不到执行宿主程序），尚未读到 `AGENTS.md`。

- 全量基线测试、真实推理及修复重试：均未执行。
- 原始响应、整数框、像素框、IoU、中心距离：均无结果。
- 未创建 `runs/qwen3_vl_anchor_integer_1000_region_20260909_001`，无 overlay 或证据文件。
- 未修改既有实验记录、桥接状态或桥接控制；未读取凭据、标签，未发布文件。

下一步：恢复执行工具后，从读取 `AGENTS.md` 和运行全部基线测试开始本轮实验。程序只能确认格式、坐标合法性和几何关系；区域语义仍须标为“待人工查看 overlay”。